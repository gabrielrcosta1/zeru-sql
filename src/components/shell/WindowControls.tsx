// Deprecated: the window now uses native OS decorations (macOS traffic lights,
// Windows/Linux native controls) via `decorations: true` in tauri.conf.json.
// Kept as an empty module because this environment can't delete the file.
export {};
